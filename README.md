# Biomachine

**Biomachine** — это платформа для .

## 📋 Описание

Biomachine — это веб-приложение, предназначенное для . Проект включает .

## ✨ Возможности


## 🛠️ Технологический стек

- **Backend**: FastAPI 0.125.0
- **База данных**: PostgreSQL 16
- **ORM**: SQLAlchemy 2.0.25
- **Шаблонизатор**: Jinja2 3.1.4
- **Безопасность**: Argon2-cffi 23.1.0
- **Валидация**: Pydantic 1.10.26
- **Сервер**: Uvicorn 0.27.1
- **Python**: 3.12

## 📁 Структура проекта

```
biomachine/
├── app/
│   ├── api/
│   │   ├── routes/
│   │   │   └── auth.py          # Маршруты аутентификации
│   │   └── deps.py              # Зависимости API
│   ├── core/
│   │   ├── config.py            # Конфигурация
│   │   ├── exceptions.py        # Обработка исключений
│   │   └── security.py          # Безопасность
│   ├── db/
│   │   ├── models/
│   │   │   └── user.py          # Модель пользователя
│   │   ├── tables/
│   │   │   └── users.py         # Таблицы БД
│   │   ├── database.py          # Настройка БД
│   │   ├── engine.py            # Движок БД
│   │   ├── init_db.py           # Инициализация БД
│   │   └── session.py           # Сессии БД
│   ├── domain/
│   │   └── user.py              # Доменные модели
│   ├── services/
│   │   └── auth.py              # Сервисы аутентификации
│   ├── static/
│   │   └── css/
│   │       └── main.css         # Стили
│   ├── templates/
│   │   ├── auth/
│   │   │   ├── login.html       # Страница входа
│   │   │   └── register.html    # Страница регистрации
│   │   ├── base.html            # Базовый шаблон
│   │   └── dashboard.html       # Дашборд
│   ├── main.py                  # Точка входа приложения
│   └── settings.py              # Настройки приложения
├── migrations/                  # Миграции базы данных
├── nginx/                       # Конфигурация Nginx
├── postgres/
│   ├── Dockerfile               # Dockerfile для PostgreSQL
│   └── init.sql                 # SQL скрипты инициализации
├── docker-compose.yaml          # Docker Compose конфигурация
├── Dockerfile                   # Dockerfile для приложения
└── requirements.txt             # Зависимости Python
```

## 🚀 Установка и запуск

### Предварительные требования

- Docker и Docker Compose
- Python 3.12+ (для локальной разработки)
- PostgreSQL 16+ (для локальной разработки)

### Запуск с Docker Compose

1. **Клонируйте репозиторий**:
   ```bash
   git clone <repository-url>
   cd biomachine
   ```

2. **Создайте файл `.env`** в корне проекта:
   ```env
   DB_URL=postgresql://biomachine_admin:strong_admin_password_here@db:5432/biomachine
   DATABASE_URL=postgresql://biomachine_admin:strong_admin_password_here@db:5432/biomachine
   ```

3. **Запустите контейнеры**:
   ```bash
   docker-compose up -d
   ```

4. **Инициализируйте базу данных**:
   ```bash
   docker-compose exec app python -m app.db.init_db
   ```

5. **Приложение будет доступно по адресу**:
   - API: http://localhost:8000
   - Документация API: http://localhost:8000/docs
   - ReDoc: http://localhost:8000/redoc

### Локальная разработка

1. **Создайте виртуальное окружение**:
   ```bash
   python -m venv venv
   source venv/bin/activate  # Linux/Mac
   # или
   venv\Scripts\activate  # Windows
   ```

2. **Установите зависимости**:
   ```bash
   pip install -r requirements.txt
   ```

3. **Настройте базу данных**:
   - Убедитесь, что PostgreSQL запущен
   - Создайте базу данных `biomachine`
   - Обновите `.env` файл с правильными учетными данными

4. **Инициализируйте базу данных**:
   ```bash
   python -m app.db.init_db
   ```

5. **Запустите приложение**:
   ```bash
   uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
   ```

## ⚙️ Конфигурация

### Переменные окружения

Создайте файл `.env` в корне проекта со следующими переменными:

```env
# URL базы данных
DB_URL=postgresql://user:password@localhost:5432/biomachine
DATABASE_URL=postgresql://user:password@localhost:5432/biomachine
```

### Настройка Docker Compose

В `docker-compose.yaml` можно изменить:
- Пароль базы данных (`POSTGRES_PASSWORD`)
- Порт PostgreSQL (по умолчанию `5432`)
- Имя базы данных (`POSTGRES_DB`)

## 📖 Использование

### API Endpoints

- `GET /` - Главная страница
- `GET /login` - Страница входа
- `GET /register` - Страница регистрации
- `GET /docs` - Интерактивная документация Swagger
- `GET /redoc` - Документация ReDoc

### Работа с API

После запуска приложения откройте http://localhost:8000/docs для интерактивной документации API, где вы можете тестировать все доступные endpoints.

## 🧪 Разработка

### Структура кода

- **API Routes** (`app/api/routes/`) - Определение маршрутов API
- **Models** (`app/db/models/`) - SQLAlchemy модели
- **Services** (`app/services/`) - Бизнес-логика
- **Core** (`app/core/`) - Основные утилиты и конфигурация
- **Templates** (`app/templates/`) - HTML шаблоны

### Добавление новых функций

1. Создайте модель в `app/db/models/`
2. Добавьте маршруты в `app/api/routes/`
3. Реализуйте бизнес-логику в `app/services/`
4. Обновите базу данных через миграции

## 🐳 Docker

### Сборка образа

```bash
docker build -t biomachine:latest .
```

### Запуск контейнера

```bash
docker run -p 8000:8000 --env-file .env biomachine:latest
```

## 📝 Лицензия

[Укажите лицензию проекта]

## 👥 Авторы

[Укажите авторов проекта]

## 🤝 Вклад в проект

Вклад в проект приветствуется! Пожалуйста, создайте issue или pull request для предложений и исправлений.

## 📞 Контакты

[Укажите контактную информацию]
